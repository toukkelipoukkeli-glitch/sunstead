# VibeNote — a Bolt/Lovable-generated note-taking app.
# Stack: Supabase only (Postgres + Auth + Storage), via the supabase-js client.
#
# This is the CANONICAL vibe-coded .env: just the project URL + anon key, with NO
# raw postgres:// connection string (Lovable/Bolt never expose it). The detector
# still recognizes it as Supabase Postgres from the *.supabase.co host.
# Aiven verdict: Postgres migrates; the crew expands Auth/Storage once it reads the DB.
#
# (Demo fixture — keys are fake.)

VITE_SUPABASE_URL=https://qktnpfdwlxmabcdef.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.demo.anon
VITE_SUPABASE_PROJECT_ID=qktnpfdwlxmabcdef
